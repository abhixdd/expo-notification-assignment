{
  "name": "expo-notification-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && npm run tunnel",
    "backend": "cd backend && npm run dev",
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "dev": "start /B npm run backend && timeout /t 3 /nobreak >nul && npm run frontend",
    "build:backend": "cd backend && npm run build",
    "test": "npm test --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
